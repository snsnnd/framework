[project]
name = "litetune-skill-code"
version = "0.5.0"
description = "Agent-facing LiteTune CLI and serial daemon scripts."
requires-python = ">=3.10"
dependencies = [
    "pyserial>=3.5",
    "pyserial-asyncio>=0.6",
]

[tool.uv]
package = false
